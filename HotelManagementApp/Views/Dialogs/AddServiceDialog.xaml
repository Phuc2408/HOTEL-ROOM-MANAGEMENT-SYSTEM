<Window x:Class="HotelManagementApp.Views.Dialogs.AddServiceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Service" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,10">
            <TextBlock Text="Service Name"/>
            <TextBox x:Name="txtServiceName" />
        </StackPanel>

        <StackPanel Margin="0,0,0,10" Grid.Row="1">
            <TextBlock Text="Unit"/>
            <TextBox x:Name="txtUnit" />
        </StackPanel>

        <StackPanel Margin="0,0,0,10" Grid.Row="2">
            <TextBlock Text="Unit Price"/>
            <TextBox x:Name="txtUnitPrice" />
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Add" Width="80" Margin="5" Click="Add_Click"/>
            <Button Content="Cancel" Width="80" Margin="5" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
