<Page x:Class="HotelManagementApp.Views.RoomManagement"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:HotelManagementApp.Views"
      xmlns:viewmodels="clr-namespace:HotelManagementApp.ViewModels"
      xmlns:models="clr-namespace:HotelManagementApp.Models"
      xmlns:converters="clr-namespace:HotelManagementApp.Converters"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:dialogs="clr-namespace:HotelManagementApp.Views.Dialogs"

      Title="RoomManagement">

    <Page.Resources>
        <converters:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <converters:FloorHighlightMultiConverter x:Key="FloorHighlightMultiConverter"/>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Room Management -->
            <RowDefinition Height="Auto"/>
            <!-- Legend -->
            <RowDefinition Height="Auto"/>
            <!-- Toolbar -->
            <RowDefinition Height="*"/>
            <!-- Layout -->
        </Grid.RowDefinitions>

        <!--  TIÊU ĐỀ -->
        <TextBlock Grid.Row="0"
               Text="Room Management"
               FontSize="24"
               FontWeight="Bold"
               Foreground="#673AB7"
               Margin="40,10,0,20"
               VerticalAlignment="Center"/>
        <StackPanel Grid.Row="1" HorizontalAlignment="Right" Margin="0,0,40,30">

            <DockPanel LastChildFill="False" VerticalAlignment="Center">

                <!-- 📅 Ngày hôm nay -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                    <materialDesign:PackIcon Kind="CalendarToday" Width="20" Height="20" Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding CurrentDateString}"
                   FontSize="14"
                   FontWeight="SemiBold"
                   VerticalAlignment="Center"/>
                </StackPanel>

                <!-- 🔍 Search -->
                <TextBox Width="200" Margin="0,0,10,0"
             materialDesign:HintAssist.Hint="Search..."
             VerticalAlignment="Center"/>

                <!-- 🔽 Sort -->
                <ComboBox Width="150" Margin="0,0,10,0"
              VerticalAlignment="Center"
              materialDesign:HintAssist.Hint="Sort">
                    <ComboBoxItem Content="Số phòng tăng dần"/>
                    <ComboBoxItem Content="Số phòng giảm dần"/>
                </ComboBox>
                <!-- Buttons -->
                <Button x:Name="EditButton" 
         Style="{StaticResource SquareIconButtonStyle}" 
         Background="Transparent" 
         Foreground="#673AB7" 
         Margin="0,0,5,0" 
         Padding="8,3" 
         BorderThickness="0">
                    <iconPacks:PackIconMaterial Kind="SquareEditOutline" Width="15" Height="15"/>
                </Button>
                <Button x:Name="DeleteButton" 
         Style="{StaticResource SquareIconButtonStyle}" 
         Background="Transparent" 
         Foreground="red" 
         Margin="0,0,5,0" 
         Padding="8,3" 
         BorderThickness="0" >
                    <iconPacks:PackIconMaterial Kind="DeleteOutline" Width="15" Height="15"/>
                </Button>
            </DockPanel>
        </StackPanel>

        <!-- Ô PHÒNG -->
        <Grid Grid.Row="3">
            <!-- giữ nguyên Grid cũ cho nút + phòng -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>


            <!-- TẦNG -->
            <StackPanel Grid.Column="0" Margin="20,0,0,0" VerticalAlignment="Center">
                <ItemsControl ItemsSource="{Binding Floors}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding}"
        Width="48" Height="48" Margin="0,12,0,12"
        Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
        Command="{Binding DataContext.ChangeFloorCommand, RelativeSource={RelativeSource AncestorType=Page}}"
        CommandParameter="{Binding}">
                                <Button.Background>
                                    <MultiBinding Converter="{StaticResource FloorHighlightMultiConverter}">
                                        <Binding />
                                        <!-- floor number -->
                                        <Binding RelativeSource="{RelativeSource AncestorType=Page}" Path="DataContext.SelectedFloor"/>
                                    </MultiBinding>
                                </Button.Background>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
            <StackPanel Grid.Column="2" Margin="10,0,10,5" VerticalAlignment="Top">
                <!-- HƯỚNG DẪN -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Border Background="Green" Width="20" Height="20" CornerRadius="3" Margin="0,0,5,0"/>
                    <TextBlock Text="In Use" VerticalAlignment="Center" Margin="0,0,15,0"/>

                    <Border Background="Gold" Width="20" Height="20" CornerRadius="3" Margin="0,0,5,0"/>
                    <TextBlock Text="Cleaning" VerticalAlignment="Center" Margin="0,0,15,0"/>

                    <Border Background="Red" Width="20" Height="20" CornerRadius="3" Margin="0,0,5,0"/>
                    <TextBlock Text="Overdue" VerticalAlignment="Center" Margin="0,0,15,0"/>

                    <Border Background="White" BorderBrush="Gray" BorderThickness="1" Width="20" Height="20" CornerRadius="3" Margin="0,0,5,0"/>
                    <TextBlock Text="Empty" VerticalAlignment="Center" Margin="0,0,15,0"/>

                    <Border Background="Gray" Width="20" Height="20" CornerRadius="3" Margin="0,0,5,0"/>
                    <TextBlock Text="Repairing" VerticalAlignment="Center" Margin="0,0,15,0"/>
                </StackPanel>
                <!-- PHÒNG -->
                <ScrollViewer Grid.Column="3" Margin="10">
                    <ItemsControl ItemsSource="{Binding FilteredRooms}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="7"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="110" Height="110"
                Margin="6"
                Padding="4"
                CornerRadius="8"
                Background="{Binding Status, Converter={StaticResource StatusToColorConverter}}"
                BorderThickness="1"
                BorderBrush="DarkGray"
                MouseLeftButtonUp="RoomBox_Click">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Effect">
                                                <Setter.Value>
                                                    <DropShadowEffect Color="Transparent" ShadowDepth="0"/>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="BorderBrush" Value="#673AB7"/>
                                                    <Setter Property="Effect">
                                                        <Setter.Value>
                                                            <DropShadowEffect Color="#888" BlurRadius="8" ShadowDepth="2"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding RoomId}" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding RoomType}" FontStyle="Italic" FontSize="12" Foreground="Gray" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding GuestName}" FontSize="12" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding CheckInDate, StringFormat='dd/MM/yyyy'}" FontSize="10" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
