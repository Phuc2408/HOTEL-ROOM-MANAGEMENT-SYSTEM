<Page x:Class="HotelManagementApp.Views.EditGuest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HotelManagementApp.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="EditService" Height="450" Width="800">
    <Grid>
        <Border Background="White" CornerRadius="10" Padding="20" Margin="20" BorderBrush="LightGray" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Title -->
                <TextBlock Text="Edit Guest" FontSize="16" FontWeight="SemiBold" Foreground="#4B5563" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>

                <!-- Form -->
                <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Guest Name -->
                    <TextBox Grid.Row="0" Grid.Column="0"
         Margin="5" Style="{StaticResource MaterialDesignOutlinedTextBox}"
         materialDesign:HintAssist.Hint="Guest name"
         Text="{Binding GuestName, UpdateSourceTrigger=PropertyChanged}" />

                    <!-- ID Card -->
                    <TextBox Grid.Row="0" Grid.Column="1"
         Margin="5" Style="{StaticResource MaterialDesignOutlinedTextBox}"
         materialDesign:HintAssist.Hint="ID Card"
         Text="{Binding IdCard, UpdateSourceTrigger=PropertyChanged}" />

                    <!-- Phone Number -->
                    <TextBox Grid.Row="1" Grid.Column="0"
         Margin="5" Style="{StaticResource MaterialDesignOutlinedTextBox}"
         materialDesign:HintAssist.Hint="Phone number"
         Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" />

                    <!-- Country -->
                    <TextBox Grid.Row="1" Grid.Column="1"
         Margin="5" Style="{StaticResource MaterialDesignOutlinedTextBox}"
         materialDesign:HintAssist.Hint="Country"
         Text="{Binding Country, UpdateSourceTrigger=PropertyChanged}" />

                    <!-- Room -->
                    <TextBox Grid.Row="2" Grid.Column="0"
         Margin="5" Style="{StaticResource MaterialDesignOutlinedTextBox}"
         materialDesign:HintAssist.Hint="Room"
         Text="{Binding Room, UpdateSourceTrigger=PropertyChanged}" />

                    <!-- Number of people -->
                    <TextBox Grid.Row="2" Grid.Column="1"
         Margin="5" Style="{StaticResource MaterialDesignOutlinedTextBox}"
         materialDesign:HintAssist.Hint="Number of people"
         Text="{Binding PeopleCount, UpdateSourceTrigger=PropertyChanged}" />

                    <!-- Check In Date -->
                    <DatePicker Grid.Row="3" Grid.Column="0"
            Margin="5" Style="{StaticResource MaterialDesignOutlinedDatePicker}"
            materialDesign:HintAssist.Hint="Check In"
            SelectedDate="{Binding CheckInDate, UpdateSourceTrigger=PropertyChanged}" />

                    <!-- Check Out Date -->
                    <DatePicker Grid.Row="3" Grid.Column="1"
            Margin="5" Style="{StaticResource MaterialDesignOutlinedDatePicker}"
            materialDesign:HintAssist.Hint="Check Out"
            SelectedDate="{Binding CheckOutDate, UpdateSourceTrigger=PropertyChanged}" />

                    <!-- Buttons -->
                
                    <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">

                        <Button Content="Save"
Margin="5" Width="100"
Background="MediumPurple" Foreground="White"
Click="SaveButton_Click" />
                        <Button Content="Cancel"
Margin="5" Width="100"
Style="{StaticResource MaterialDesignOutlinedButton}"
Click="CancelButton_Click" />
                    </StackPanel>
                </Grid>
            </Grid>

        </Border>
        
    </Grid>
</Page>
